// OrbitPath — SCAFFOLD STUB.
// v1 will render the elliptical orbit line for a body, honoring the
// true Ω/i/ω transform from lib/orbital.js (not the POC's x-tilt).
export function OrbitPath() {
  return null;
}
