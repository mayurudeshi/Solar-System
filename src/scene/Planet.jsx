// Planet — SCAFFOLD STUB.
// v1 will:
//   - take orbital elements + simulation date from useStore
//   - compute position via keplerianToCartesian()
//   - render textured sphere with axial tilt
//   - emit click → setSelected(name)
//   - hold ring slot for Saturn
//
// For the scaffold, this file exists only so the import graph compiles.
export function Planet() {
  return null;
}
